# Initialization
1. In the **root of the repo run `corepack enable` then `yarn install`

# Development

### Local API
**From the root of the project**
1. Run `yarn api:build`
2. Run `yarn api:dev` to start the API server locally


